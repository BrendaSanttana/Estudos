// tabuada

/* let numero = 4;

//for (inicialização, condição de repetição, incremento/decremetno) {}

for (let contador = 1; contador <= 10; contador +=1) {
    console.log (numero * contador);
}
// saudações 
let nomes = ["Brenda", "Carol", "Thaina", "Daniel", "Robs", "Rogerio", "Cleide"]

for (let indice = 0; indice < nomes.l; indice +=1) {
     console.log ("Olá, " + nomes[indice])
}
*/

let groceryList = ['Arroz', 'Feijão', 'Alface', 'Melancia', 'Frango', 'Queijo'];

for (let index = 0; index < groceryList.length; index +=1) {
    let element = groceryList[index];
    console.log(element)
}